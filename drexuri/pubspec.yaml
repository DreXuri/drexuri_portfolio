name: drexuri
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'


dependencies:
  flutter:
    sdk: flutter


  # cupertino_icons: ^1.0.2
  # This is a library for interacting with GitHub  in flutter
  github: ^9.2.0
  #composable, Future-based library for making HTTP requests
  http: ^0.13.1
  # provider for state management
  provider: ^6.0.5
  # Flutter plugin for reading and writing simple key-value pairs
  shared_preferences: ^2.2.0
  # # google fonts utils
  google_fonts: ^3.0.1
  # # Flutter plugin for launching a URL. Supports web, phone, SMS, and email schemes.
  url_launcher: ^6.1.2
  # #utils
  hexcolor: ^2.0.7
  # state management
  provider: ^6.0.5

  # utils
  mailto: ^2.0.0
  flip_card: ^0.6.0
  cupertino_icons: ^1.0.2
  carousel_slider: ^4.0.0
  animated_text_kit: ^4.2.1

  # web utils
  flutter_html: any
  url_strategy: ^0.2.0
  universal_html: ^2.0.4



 

  # cache
  # shared_preferences: ^2.0.6


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true


  assets:
    - assets/images/
    - assets/icons/